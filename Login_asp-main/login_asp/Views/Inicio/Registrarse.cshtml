@model login_asp.Models.Usuario;
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Registrarse</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css">
    <style>
        body {
            background-color: #aaffaa; /* Cambiar el color de fondo de toda la página a verde claro */
        }

        .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            background-color: #e0ffe0; /* Cambiar el color de fondo de las tarjetas a verde claro */
        }

        .card-header {
            background-color: #00aa00; /* Cambiar el color del encabezado de las tarjetas a verde oscuro */
            color: #fff;
            border-radius: 15px 15px 0 0;
            text-align: center;
            font-size: 24px;
            padding: 1rem 0;
        }

        .form-control {
            border: 2px solid #aaffaa; /* Cambiar el color del borde de los campos de entrada a verde claro */
            border-radius: 12px;
            background-color: #e0ffe0; /* Cambiar el color de fondo de los campos de entrada a verde claro */
        }

        .btn-primary {
            background-color: #00aa00; /* Cambiar el color de los botones a verde oscuro */
            border: none;
            border-radius: 12px;
            padding: 10px 20px;
            color: #fff; /* Cambiar el color del texto de los botones a blanco */
        }

            .btn-primary:hover {
                background-color: #008800; /* Cambiar el color de los botones al pasar el ratón por encima a verde más oscuro */
            }

        .alert {
            background-color: #55ff55; /* Cambiar el color de fondo de las alertas a verde claro */
            border-radius: 12px;
            color: #000; /* Cambiar el color del texto dentro de las alertas a negro */
            padding: 10px; /* Añadir un poco de espacio interno para que se vea mejor */
        }
    </style>
</head>
<body>

    <div class="container mt-5 pt-5">
        <div class="row justify-content-center">
            <div class="col-12 col-xl-4 col-md-6">
                <div class="card">
                    <div class="card-header">
                        Regístrate
                    </div>
                    <div class="card-body">
                        <form asp-controller="Inicio" asp-action="Registrarse" method="post">
                            <div class="mb-3">
                                <input class="form-control" type="text" placeholder="Nombre Completo" asp-for="NombreUsuario" required>
                            </div>
                            <div class="mb-3">
                                <input class="form-control" type="text" placeholder="Apellido" asp-for="Apellido" required>
                            </div>
                            <div class="mb-3">
                                <input class="form-control" type="text" placeholder="Correo" asp-for="Correo" required>
                            </div>
                            <div class="mb-3">
                                <input class="form-control" type="password" placeholder="Contraseña" required asp-for="Clave">
                            </div>
                            <div class="d-grid mb-3">
                                <button class="btn btn-primary" type="submit">Regístrate</button>
                            </div>
                            @if (ViewData["Mensaje"] != null)
                            {
                                <div class="alert alert-danger" role="alert">
                                    @ViewData["Mensaje"]
                                </div>
                            }
                            ¿Ya tienes una cuenta? <a href="@Url.Action("IniciarSesion","Inicio")">Iniciar Sesión</a>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

